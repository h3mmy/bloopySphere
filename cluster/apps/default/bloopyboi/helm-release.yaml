apiVersion: helm.toolkit.fluxcd.io/v2beta2
kind: HelmRelease
metadata:
  name: bloopyboi
  namespace: default
spec:
  interval: 5m
  chart:
    spec:
      chart: bloopyboi
      version: 0.1.1
      sourceRef:
        kind: HelmRepository
        name: bloopy-charts
        namespace: flux-system
      interval: 5m
  install:
    remediation:
      retries: 4
  upgrade:
    remediation:
      remediateLastFailure: true
  values:
    env:
      TZ: ${TZ}
    image:
      repository: registry.gitlab.com/h3mmy/bloopyboi
      tag: v0.4.0
  valuesFrom:
    - kind: Secret
      name: bloopyboi-secrets
      targetPath: bloopyboi.botToken
      valuesKey: botToken
    - kind: Secret
      name: bloopyboi-secrets
      targetPath: bloopyboi.appId
      valuesKey: appId
