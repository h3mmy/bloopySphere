---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: hugo-1-config-v1
  namespace: blogs
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: ceph-block
