---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: ghost-admin
  namespace: blogs
spec:
  entryPoints:
    - websecure
  routes:
    - kind: Rule
      match: Host(`tygr.${XYZ_DOMAIN}`) && PathPrefix(`/ghost`)
      priority: 100
      services:
        - kind: Service
          name: ghost
          port: 2368
      middlewares:
        - name: networking-rfc1918-ips@kubernetescrd
        - name: networking-secure-headers-tgr@kubernetescrd
  tls:
    secretName: "tls.${XYZ_DOMAIN/./-}"
