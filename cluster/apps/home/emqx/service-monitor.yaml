---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: emqx-metrics
  namespace: home
  labels:
    k8s-app: emqx
spec:
  selector:
    matchLabels:
      k8s-app: emqx
  endpoints:
    - port: 9091
      path: /api/v4/emqx_prometheus
      interval: 30s
