---
apiVersion: traefik.containo.us/v1alpha1
kind: TLSStore
metadata:
  name: default
  namespace: networking
spec:
  certificates:
    - secretName: tls.${TGR_DOMAIN/./-}
    - secretName: tls.${SECRET_DOMAIN/./-}
  defaultCertificate:
    secretName: "tls.${XYZ_DOMAIN/./-}"
