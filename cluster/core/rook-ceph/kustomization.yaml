---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # Pre Flux-Kustomizations
  - ./namespace.yaml
  # Flux-Kustomizations
  - ./rook-ceph/ks.yaml
  # Post Flux Kustomizations
  # temporarily disable until we can refactor for rook 1.19.11 and 1.20.x
  # - ./rook-ceph/tools
